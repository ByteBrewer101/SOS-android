# Dependencies — reinstalled inside the image
node_modules/

# Local secrets — injected via environment variables on the VPS, never baked in
.env

# Logs
*.log
logs/

# OS noise
.DS_Store
Thumbs.db

# Dev/CI artifacts
coverage/
dist/

# Docker files themselves
Dockerfile
.dockerignore
docker-compose*.yml

# Docs & config not needed at runtime
CLAUDE.md
README.md
